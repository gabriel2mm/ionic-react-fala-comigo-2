import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.falacomigo2',
  appName: 'fala-comigo2',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
